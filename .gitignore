config.js
.claude/settings.local.json
Thumbs.db
.DS_Store
git push -u origin main

# JPEGs crudos de Google Flow para las medallas de logros (24MB, fondo
# blanco sin procesar) — solo insumo de scripts/process-achievement-badges.js;
# lo que la app realmente usa son los PNG ya recortados en assets/logros/,
# que sí se versionan.
assets/Iconos Logros/

# Capacitor / Android
node_modules/
www/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/app/release/
*.keystore
*.jks
